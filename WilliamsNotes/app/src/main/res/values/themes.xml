<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Theme.WilliamsNotes" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/monte_carlo</item>
        <item name="colorPrimaryVariant">@color/monte_carlo</item>
        <item name="colorOnPrimary">@color/espresso</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/tulip_tree</item>
        <item name="colorSecondaryVariant">@color/tulip_tree</item>
        <item name="colorOnSecondary">@color/pale_espresso</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/monte_carlo</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/monte_carlo</item>
    </style>

</resources>
