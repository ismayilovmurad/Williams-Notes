<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Theme.WilliamsNotes" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/espresso</item>
        <item name="colorPrimaryVariant">@color/espresso</item>
        <item name="colorOnPrimary">@color/tulip_tree</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/pale_espresso</item>
        <item name="colorSecondaryVariant">@color/pale_espresso</item>
        <item name="colorOnSecondary">@color/monte_carlo</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/espresso</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/espresso</item>
    </style>

</resources>
